/**
 * Zod schemas — the single source of truth for validation and types.
 *
 * Define a schema here, then infer its type in `../types`. Never hand-write a
 * DTO type that duplicates one of these schemas.
 *
 * Note: inferred types are re-exported from `../types`, not here, to avoid
 * duplicate re-exports in the package barrel.
 */
export {};
