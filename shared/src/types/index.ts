/**
 * TypeScript types inferred from Zod schemas.
 *
 * These are re-exports of `z.infer` results for ergonomic imports. Do not
 * hand-write types here that duplicate a schema in `../validation`.
 */

/**
 * Shared type exports are defined here.
 * Add inferred types from validation schemas as needed.
 */
